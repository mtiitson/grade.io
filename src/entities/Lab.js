export default class Lab {
    constructor(number, title, description, date) {
        this.number = number;
        this.title = title;
        this.description = description;
        this.date = date;
    }
}
